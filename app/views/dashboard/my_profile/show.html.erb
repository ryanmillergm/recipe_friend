<%= render '/dashboard/dashboard/dash_nav' %>

<div class="my-profile-container">
  <div class="my-profile-header">
    <div class="my-profile-background-image">
      <% if @user.background_image.attached? %>
        <%= image_tag(url_for(@user.cropped_background)) %>
        <%= link_to 'Edit Cover Photo', user_crop_image_path(@user.id) %>
      <% else %>
      <%=image_tag("user-background-image.jpg", height: '200')%>
      <br>
      <div class="my-cover-photo-link">
        <%= link_to 'Add Cover Photo', edit_user_crop_image_path(@user.id) %>
      </div>
      <% end %>
      <div class="my-profile-picture">
        <% if @user.avatar.attached? %>
          <%= image_tag(url_for(@user.cropped_avatar), height: '125', :id => "my-profile-picture") %>
          <%= link_to 'Edit Profile Photo', new_user_crop_image_path(@user.id) %>
        <% else %>
          <%=image_tag("user_image.png", height: '100')%>
          <br>
          <%= link_to 'Add Profile Photo', edit_dashboard_my_profile_path(@user.id) %>
        <% end %>
      </div>
      <div class="my-profile-info">
        <div class="username">
          <%= @user.username %>
        </div>
        <div class="location">
          <p>
            Location: <%= @user.location %>
          </p>
        </div>
        <div class="quote">
          <p>
            Favorite Quote: "<%= @user.quote %>"
          </p>
        </div>
        <div class="about">
          <p>
            About Me: <%= @user.about %>
          </p>
        </div>
        <div id="edit-profile">
          <%= link_to 'Edit Profile', edit_dashboard_my_profile_path(@user.id) %>
        </div>
      </div>
    </div>
  </div>
</div>
